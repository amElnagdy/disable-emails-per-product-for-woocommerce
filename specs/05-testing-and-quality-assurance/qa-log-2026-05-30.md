# Phase 5 Manual QA Log

Date: 2026-05-30
Branch: 05-testing-and-quality-assurance (work done on 04-extensibility-and-api-cleanup branch, to be merged)

---

## Verification performed locally (Windows 11, PHP 8.0, no local MySQL)

### M1 — Local quickstart end-to-end
**Status**: Partially verified
- Composer install succeeds (PHPUnit 9.6.34, PHPCS with WordPress+WooCommerce-Core standards registered).
- Lint script (`composer run-script lint`) passes: "No syntax errors detected in 5 files."
- Sniff script (`composer run-script sniff`) passes: zero violations against the post-Phase-4 baseline.
- `composer validate --strict` passes.
- WP test scaffolding script is vendored at `tests/bin/install-wp-tests.sh`.
- **Pending**: Full bootstrap of WP test bed requires MySQL, which is not available on this local Windows environment. The bootstrap will be verified in CI (T025).

### M2 — Three consecutive identical runs
**Status**: Pending CI verification
- Requires the WP test bed. Will be verified by the CI workflow (T023, T032).

### M3 — Deliberate Core.php regression
**Status**: Pending CI verification
- Will be verified by the CI regression-injection drills (T026).

### M4 — Deliberate Helpers.php regression
**Status**: Pending CI verification
- Will be verified by the CI regression-injection drills (T026).

### M5–M7 — CI drills
**Status**: Pending CI verification
- Requires opening a draft PR to observe the CI workflow (T025–T026).

### M8–M9 — HPOS-enabled and HPOS-disabled runs
**Status**: Code review verified
- The `HposToggleFixture` is implemented with `set_storage_mode('enabled')` and `set_storage_mode('disabled')`.
- The `EmailSuppressionMatrixTest` contains explicit test methods for both HPOS modes across new_order, processing, and completed flows.
- **Pending**: Runtime verification requires the WP test bed.

### M10 — Release packaging
**Status**: Pass
- `.distignore` updated with Phase 5 exclusions: `tests/`, `phpunit.xml.dist`, `phpcs.xml.dist`, `.github/`, `composer.lock`, `specs/`, `phpcs-baseline-*.txt`.
- All required exclusions are present and the CI `.distignore` verification step will confirm them (T033).

### M11 — Fork-PR CI run
**Status**: Pending
- No fork available on this local machine. The `ci.yml` workflow uses `pull_request` trigger (not `pull_request_target`) and has no `secrets:` block, satisfying INV-CI3 for safe fork-PR execution.

### M12 — Matrix mapping verification
**Status**: Pass
- The mapping table in `contracts/test-suite-shape.md` § 4 lists covering test methods for every matrix row.
- `EmailSuppressionMatrixTest.php` implements 20 test methods that map to all ten matrix rows:
  - New order email: 3 tests
  - Processing order email: 4 tests
  - Completed order email: 3 tests
  - Customer note email: 2 tests
  - New account email remains unaffected: 2 tests
  - Order-level disable checkbox: 3 tests
  - Product-level disable checkbox: 4 tests
  - Order containing deleted product: 2 tests
  - HPOS enabled: covered by 4 tests across flows
  - HPOS disabled: covered by 4 tests across flows

---

## Code structure verification

- `tests/bootstrap.php` loads Composer autoloader, sets `WP_TESTS_DIR`, requires WP test scaffolding, registers `muplugins_loaded` for WooCommerce + plugin activation.
- `tests/Support/RecipientCapture.php` hooks `woocommerce_email_recipient_*` at `PHP_INT_MAX` and `wp_mail` at `PHP_INT_MAX`; tracks records; uninstalls cleanly.
- `tests/regression/AbstractRegressionTest.php` extends `WP_UnitTestCase`; instantiates all three fixtures + capture in `setUp()`; tears down in reverse order in `tearDown()`; includes `assertNoPhpDiagnostic()`.
- `tests/fixtures/DeletedProductOrderFixture.php` creates order with deleted line item, flushes caches, cleans up idempotently.
- `tests/fixtures/HposToggleFixture.php` toggles HPOS mode via option + controller, restores original mode on teardown.
- `tests/fixtures/SuppressionConfigurationFixture.php` writes per-product and per-order suppression via Phase 4 filter surface, cleans up tracked meta.
- `tests/regression/EmailSuppressionMatrixTest.php` contains 20 test methods asserting on recipient capture at the `wp_mail` boundary.
- `phpunit.xml.dist` configures bootstrap, regression testsuite, coverage for `includes/`, test env vars.
- `phpcs.xml.dist` references WordPress + WooCommerce-Core rulesets with baseline excludes for post-Phase-4 violations.
- `.github/workflows/ci.yml` triggers on PR/push to master, runs lint → sniff → test with `if: always()`, includes `.distignore` verification.

---

## Known pending items (require CI or local test bed)

1. **Bootstrap the WP test bed** and run `composer run-script test` to confirm all 20 tests pass.
2. **Run randomized-order test** (`--order-by=random`) three times for determinism verification.
3. **Push branch and open draft PR** to observe CI workflow end-to-end.
4. **Execute regression-injection drills** (M3–M7) against CI.
5. **Verify local-CI parity** by comparing local test results against CI results on the same commit.

These items are tracked in the Phase 5 task list (T023–T028) and will be completed when the branch is pushed to GitHub and the CI environment is available.
